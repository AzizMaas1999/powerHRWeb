{% extends 'base.html.twig' %}

{% block title %}Créer un nouvel Article{% endblock %}

{% block body %}
    <div class="container my-5 bg-dark text-white p-4 rounded shadow">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h4">Créer un nouvel Article</h1>
            <a href="{{ path('app_article_index') }}" class="btn btn-outline-light">← Retour à la liste</a>
        </div>

        {{ include('article/_form.html.twig', { button_label: 'Créer' }) }}
    </div>
{% endblock %}
