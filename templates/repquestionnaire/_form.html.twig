{{ form_start(form, {'attr': {'class': 'row g-3'}}) }}

    {% for field in form %}
        <div class="col-md-6">
            {{ form_row(field) }}
        </div>
    {% endfor %}

    <div class="col-12">
        <button class="btn btn-success">Enregistrer</button>
    </div>

{{ form_end(form) }}
