{% extends 'base.html.twig' %}

{% block title %}Départements{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-6 fw-bold text-info mb-0">📋 Liste des Départements</h1>

        <a href="{{ path('app_departement_new') }}" class="btn btn-info btn-sm btn-round">
            ➕ Nouveau Département
        </a>
    </div>

    {% if departements is empty %}
        <div class="alert alert-warning text-center fw-semibold">
            Aucun département trouvé.
        </div>
    {% else %}
        <div class="table-responsive shadow-sm rounded">
            <table class="table table-hover align-middle mb-0 bg-white">
                <thead class="bg-light text-dark fw-semibold">
                    <tr>
                        <th>ID</th>
                        <th>Nom</th>
                        <th>Description</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for departement in departements %}
                        <tr>
                            <td>{{ departement.id }}</td>
                            <td>{{ departement.nom }}</td>
                            <td>
                                {% if departement.description|length > 60 %}
                                    {{ departement.description|slice(0, 60) ~ '...' }}
                                {% else %}
                                    {{ departement.description }}
                                {% endif %}
                            </td>
                            <td class="text-end">
                                <a href="{{ path('app_departement_show', {'id': departement.id}) }}" class="btn btn-sm btn-outline-info me-1" title="Voir">
                                    <i class="fa fa-eye"></i>
                                </a>
                                <a href="{{ path('app_departement_edit', {'id': departement.id}) }}" class="btn btn-sm btn-outline-warning me-1" title="Modifier">
                                    <i class="fa fa-pencil"></i>
                                </a>
                                <a href="{{ path('app_departement_delete', {'id': departement.id}) }}" class="btn btn-sm btn-outline-danger" title="Supprimer">
                                    <i class="fa fa-trash"></i>
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
</div>
{% endblock %}
