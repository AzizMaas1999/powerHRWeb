<div class="container mt-4">
    <div class="card shadow p-4">
            <h2 class="text-center mb-4 text-primary">Créer un nouveau questionnaire</h2>

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

 <div class="row justify-content-center">
            <div class="col-md-8 mb-3">
        {{ form_label(form.objet, 'Objet', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.objet, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.objet) }}
    </div>
</div>

 <div class="row justify-content-center">
            <div class="col-md-8 mb-3">
        {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.description) }}
    </div>
</div>
  <div class="text-center mt-4">
            <button type="submit" class="btn btn-primary">
                {{ button_label|default('Enregistrer') }}
            </button>
            <a href="{{ path('app_questionnaireadmin_index') }}" class="btn btn-secondary">Retour</a>
        </div>
{{ form_end(form) }}
</div>
</div>