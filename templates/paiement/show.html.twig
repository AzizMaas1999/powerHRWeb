{% extends 'base.html.twig' %}

{% block title %}Détail du Paiement{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow rounded">
        <div class="card-header bg-primary text-white">
            <h3 class="mb-0">Détails du Paiement</h3>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th scope="row">Date</th>
                        <td>{{ paiement.date ? paiement.date|date('Y-m-d') : 'Non définie' }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Mode</th>
                        <td>{{ paiement.mode|capitalize }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Référence</th>
                        <td>{{ paiement.reference }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Montant</th>
                        <td>{{ paiement.montant }} €</td>
                    </tr>
                </tbody>
            </table>

            <div class="mt-4 d-flex justify-content-between">
                <a href="{{ path('app_paiement_index') }}" class="btn btn-outline-secondary">
                    ← Retour à la liste
                </a>

                <div>
                    <a href="{{ path('app_paiement_edit', {'id': paiement.id}) }}" class="btn btn-warning me-2">
                        ✏️ Modifier
                    </a>

                    {{ include('paiement/_delete_form.html.twig', { 'button_label': '🗑️ Supprimer', 'button_class': 'btn btn-danger' }) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
