{# templates/paiement/error.html.twig #}

<!DOCTYPE html>
<html>
<head>
    <title>Erreur de paiement</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f8f8;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .error-message {
            color: red;
            font-size: 1.2em;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Erreur de paiement</h1>
        <p class="error-message">{{ message }}</p>
        <p><a href="{{ path('app_facture_index') }}">Retour à la liste des factures</a></p>
    </div>
</body>
</html>
