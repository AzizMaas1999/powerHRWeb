<form method="post" action="{{ path('app_fiche_employe_delete', {'id': fiche_employe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_employe.id) }}">
    <button class="btn">Delete</button>
</form>
