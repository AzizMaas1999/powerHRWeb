{% extends 'base.html.twig' %}

{% block title %}Ajouter une Fiche Employé{% endblock %}

{% block body %}
<div class="container">
    <h2>Ajouter une Fiche Employé</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

    <a href="{{ path('app_fiche_employe_index') }}" class="btn btn-secondary mt-3">Retour</a>
</div>
{% endblock %}
