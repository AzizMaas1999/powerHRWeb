{{ form_start(form) }} {# Pas de 'novalidate' ici #}

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-4 p-4 bg-dark text-white">
        <div class="card-body">
            <h2 class="mb-4 text-center">
                <i class="fas fa-plus-circle me-2"></i> Nouveau CLFR
            </h2>

            {{ form_row(form.nom) }}
            {{ form_row(form.matricule_fiscale) }}
            {{ form_row(form.adresse) }}
            {{ form_row(form.numTel) }}
            {{ form_row(form.type) }}
            {{ form_row(form.photoPath) }}

            <div class="text-center mt-4">
                <button type="submit" class="btn btn-primary">Créer</button>
                <a href="{{ path('app_clfr_index') }}" class="btn btn-outline-light">
                    <i class="fas fa-arrow-left me-1"></i> Retour à la liste
                </a>
            </div>
        </div>
    </div>
</div>

{{ form_end(form) }}