<form method="post" action="{{ path('app_clfr_delete', {'id': clfr.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clfr.id) }}">
    <button class="btn">Delete</button>
</form>
