{% extends 'base.html.twig' %}

{% block title %}Employe{% endblock %}

{% block body %}
    <h1>Employe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employe.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ employe.username }}</td>
            </tr>
            
            <tr>
                <th>Poste</th>
                <td>{{ employe.poste.value }}</td>
            </tr>
            <tr>
                <th>Salaire</th>
                <td>{{ employe.salaire }}</td>
            </tr>
            <tr>
                <th>Rib</th>
                <td>{{ employe.rib }}</td>
            </tr>
            <tr>
                <th>CodeSociale</th>
                <td>{{ employe.codeSociale }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employe_index') }}">back to list</a>

    <div class="mt-4 d-flex justify-content-center gap-3">
                <a href="{{ path('app_employe_edit', {'id': employe.id}) }}" class="btn btn-success px-4">
                    Modifier
                </a>

                {{ include('employe/_delete_form.html.twig') }}
            </div>

    

    
    
{% endblock %}
