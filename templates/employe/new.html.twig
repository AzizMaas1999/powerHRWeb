{% extends 'base.html.twig' %}

<<<<<<< HEAD
{% block title %}Créer un nouveau CLFR{% endblock %}

{% block body %}
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-4 p-4 bg-dark text-white">
        <div class="card-body">
            <h2 class="mb-4 text-center">
                <i class="fas fa-plus-circle me-2"></i> Nouveau CLFR
            </h2>

            {% if not form.vars.valid %}
                <div class="alert alert-danger">
                    <strong>Veuillez corriger les erreurs ci-dessous.</strong>
                </div>
            {% endif %}

            <div class="form-group mb-3">
                {{ form_label(form.nom, 'Nom', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.nom, {'attr': {'class': 'form-control' ~ (form.nom.vars.valid ? '' : ' is-invalid')}}) }}
                {% if not form.nom.vars.valid %}
                    <div class="invalid-feedback">
                        {% for error in form.nom.vars.errors %}
                            {{ error.message }}
                        {% endfor %}
                    </div>
                {% endif %}
            </div>

            <div class="form-group mb-3">
                {{ form_label(form.matricule_fiscale, 'Matricule fiscale', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.matricule_fiscale, {'attr': {'class': 'form-control' ~ (form.matricule_fiscale.vars.valid ? '' : ' is-invalid')}}) }}
                {% if not form.matricule_fiscale.vars.valid %}
                    <div class="invalid-feedback">
                        {% for error in form.matricule_fiscale.vars.errors %}
                            {{ error.message }}
                        {% endfor %}
                    </div>
                {% endif %}
            </div>

            <div class="form-group mb-3">
                {{ form_label(form.adresse, 'Adresse', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.adresse, {'attr': {'class': 'form-control' ~ (form.adresse.vars.valid ? '' : ' is-invalid')}}) }}
                {% if not form.adresse.vars.valid %}
                    <div class="invalid-feedback">
                        {% for error in form.adresse.vars.errors %}
                            {{ error.message }}
                        {% endfor %}
                    </div>
                {% endif %}
            </div>

            <div class="form-group mb-3">
                {{ form_label(form.numTel, 'Numéro de téléphone', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.numTel, {'attr': {'class': 'form-control' ~ (form.numTel.vars.valid ? '' : ' is-invalid')}}) }}
                {% if not form.numTel.vars.valid %}
                    <div class="invalid-feedback">
                        {% for error in form.numTel.vars.errors %}
                            {{ error.message }}
                        {% endfor %}
                    </div>
                {% endif %}
            </div>

            <div class="form-group mb-3">
                {{ form_label(form.type, 'Type', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.type, {'attr': {'class': 'form-control' ~ (form.type.vars.valid ? '' : ' is-invalid')}}) }}
                {% if not form.type.vars.valid %}
                    <div class="invalid-feedback">
                        {% for error in form.type.vars.errors %}
                            {{ error.message }}
                        {% endfor %}
                    </div>
                {% endif %}
            </div>

            <div class="form-group mb-3">
                {{ form_label(form.photoPath, 'Photo', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.photoPath, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="text-center mt-4">
                <button type="submit" class="btn btn-primary">{{ button_label|default('Créer') }}</button>
                <a href="{{ path('app_clfr_index') }}" class="btn btn-outline-light">
                    <i class="fas fa-arrow-left me-1"></i> Retour à la liste
                </a>
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}
=======
{% block title %}Nouveau Employe{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-white">Créer un nouveau employe</h1>
        <a href="{{ path('app_employe_index') }}" class="btn btn-sm btn-primary">← Retour à la liste</a>
    </div>

    {{ include('employe/_form.html.twig', { button_label: 'Enregistrer' }) }}
</div>
>>>>>>> origin/yassine
{% endblock %}
