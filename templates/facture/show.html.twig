{% extends 'base.html.twig' %}

{% block title %}Détail de la facture{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-info text-white">
            <h2 class="mb-0">Détails de la facture</h2>
        </div>
        <div class="card-body">
            <table class="table table-striped">
                <tbody>
                    <tr>
                        <th scope="row">Date</th>
                        <td>{{ facture.date ? facture.date|date('d/m/Y') : 'Non définie' }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Type de Facture</th>
                        <td>{{ facture.typeFact }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Numéro</th>
                        <td>{{ facture.num }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Total (€)</th>
                        <td>{{ facture.total|number_format(2, ',', ' ') }}</td>
                    </tr>
                </tbody>
            </table>

            <div class="d-flex justify-content-between mt-4">
                <a href="{{ path('app_facture_index') }}" class="btn btn-secondary">
                    ⬅ Retour à la liste
                </a>

                <div>
                    <a href="{{ path('app_facture_edit', {'id': facture.id}) }}" class="btn btn-warning me-2">
                        ✏️ Modifier
                    </a>

                    {{ include('facture/_delete_form.html.twig', { 'button_class': 'btn btn-danger' }) }}
                </div>
            </div>

            <div class="text-center mt-4">
                <a href="{{ path('app_facture_paiement', {'id': facture.id}) }}" class="btn btn-success btn-lg">
                    💳 Payer cette facture
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
