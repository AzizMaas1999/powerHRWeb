{% extends 'base.html.twig' %}

{% block title %}Liste des Factures{% endblock %}

{% block body %}
<div class="container py-4 text-white">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">📄 Liste des Factures</h1>
        <a href="{{ path('app_facture_new') }}" class="btn btn-success">
            ➕ Nouvelle Facture
        </a>
    </div>

    <div class="table-responsive shadow-sm rounded">
        <table class="table table-dark table-hover align-middle text-center">
            <thead class="table-secondary text-dark">
                <tr>
                    <th>Date</th>
                    <th>Type</th>
                    <th>Numéro</th>
                    <th>Total</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for facture in factures %}
                <tr>
                    <td>{{ facture.date ? facture.date|date('Y-m-d') : '' }}</td>
                    <td>{{ facture.typeFact }}</td>
                    <td>{{ facture.num }}</td>
                    <td>{{ facture.total }} €</td>
                    <td>
                        <a href="{{ path('app_facture_show', {'id': facture.id}) }}" class="btn btn-outline-light btn-sm me-1">
                            Voir
                        </a>
                        <a href="{{ path('app_facture_edit', {'id': facture.id}) }}" class="btn btn-outline-warning btn-sm">
                            Modifier
                        </a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="text-muted">Aucune facture trouvée.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
